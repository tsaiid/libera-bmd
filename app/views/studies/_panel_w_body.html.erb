      <div class="panel panel-default">
        <div class="panel-heading">
          <h3 class="panel-title">
            <a data-toggle="collapse" data-parent="#studies-panel" href="#collapse-<%= study.scanid %>">
              <%= study.to_s %>
            </a>
          </h3>
        </div>
        <div id="collapse-<%= study.scanid %>" class="panel-collapse collapse in">
          <div class="panel-body">
            <table class="table table-condensed">
              <thead>
                <tr>
                  <th>Region</th>
                  <th>Area <small>(cm<sup>2</sup>)</small></th>
                  <th>BMC <small>(g)</small></th>
                  <th>BMD <small>(g/cm<sup>2</sup>)</small></th>
                  <th>Fat <small>(g)</small></th>
                  <th>Lean <small>(g)</small></th>
                  <th>Lean + BMC <small>(g)</small></th>
                  <th>Total <small>(g)</small></th>
                  <th>% Fat <small>(%)</small></th>
                  <th>T-score</th>
                  <th>PR <small>(%)</small></th>
                  <th>Z-score</th>
                  <th>AM <small>(%)</small></th>
                </tr>
              </thead>
              <tbody>
                <% study.exam.levels.each do |level| %>
                <tr class="<%= "active" if level[:label] == "Total" %>">
                  <td><%= level[:label] %></td>
                  <td><%= level[:area].round(2) %></td>
                  <td><%= level[:bmc].round(2) %></td>
                  <td><%= level[:bmd].round(3) %></td>
                  <td></td>
                  <td></td>
                  <td></td>
                  <td></td>
                  <td></td>
                  <td><%= level[:t_score].round(1) if level[:label] == "Total" %></td>
                  <td><%= level[:peak_reference].round(0) if level[:label] == "Total" %></td>
                  <td><%= level[:z_score].round(1) if level[:label] == "Total" %></td>
                  <td><%= level[:age_matched].round(0) if level[:label] == "Total" %></td>
                </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
